
<!-- README.md is generated from README.Rmd. Please edit that file -->

# BIT’s data analysis toolbox

This repository is a collection of R scripts and functions, implementing
common data analysis tasks at BIT’s Predictiv team. It is heavily
focused on functionality that is used to analyze data from Predictiv
surveys. Though we are generally open to contributions (see
“Contributing” sections), they should be in line with the package’s
scope. Pull requests and feature requests that are out of scope (e.g.,
niche and/or one-off analyses) will be rejected.

All analysis and parameter defaults were chosen based on the
recommendations given in the Blue Book v1.5 (internal methodology
guide).

## Features

A graphical overview of the package can be found under
`doc/architecture-diagram`.

The following modules are currently implemented:

- **Data loading** module: load data from a local `csv` file, pull data
  from a remote `DuckLake` database
- **Template** module: make a local copy of bundled templates (e.g.,
  Predictiv cleaning template for CINT data)

Note on templates: templates are **not** ready for consumption as is.
Instead, they have to be adjusted for the individual use case (adjusting
variable names, toggling analysis steps, etc.), after being copied with
the `use_template` function. Templates only provide a scaffold.

## Repository structure

    bittoolbox/
    ├── R/                                   # Package source code
    │   ├── data.R                           # Bundled dataset documentation
    │   ├── import_data.R                    # Data loading functions
    │   └── use_template.R                   # Template copying function
    ├── data/                                # Bundled example datasets (.rda)
    ├── inst/
    │   └── templates/                       # Bundled templates
    ├── doc/
    │   └── architecture-diagram.html
    ├── man/                                 # Auto-generated function reference docs (roxygen2)
    ├── tests/                               # Unit tests
    ├── DESCRIPTION                          # Package metadata and dependencies
    ├── NAMESPACE                            # Exported functions (auto-generated by roxygen2)
    └── README.Rmd                           # Source for README.md — edit this, not README.md

## Getting started

### Installation

You can install the development version of bittoolbox from
[GitHub](https://github.com/BehaviouralInsights/data-analysis-toolbox)
with:

``` r
# install.packages("pak")
pak::pak("BehaviouralInsights/data-analysis-toolbox")
```

### Example

This is a basic example for an end-to-end analysis pipeline, using
functions, bundled with this package:

``` r
library(bittoolbox)

# --- Step 1: Set up a data cleaning template ---
# Templates are scaffolds and must be adapted for your survey
# (adjust variable names, toggle analysis steps, etc.)
bittoolbox::use_template("predictiv_cint_cleaning")
#> Template `predictiv_cint_cleaning`: 1 file copied to './predictiv_cint_cleaning'

# --- Step 2: Load example data ---
data <- bittoolbox::ai_can_take_your_job_not_mine
```

## Contributing

For features requests, bug reports, etc., please use the `Issues` tab in
the repository. This is the preferred way to contribute to this package.

Please note though that this only applies to core-functionality of the
package: bundled templates are not covered by this, and are **not**
maintained by the main maintainers of this repo. If you want to change
an existing template, please clone this repo, make your changes to the
template, and create a PR (see below); If you want to make a suggestion
for a new template to be bundled, please open an issue and link to the
files you want to bundle with this package.

Another (optional and more involved) way to contribute is to contribute
your own custom functions. To do this, please clone this repo, and
branch off `main` to create a feature branch. Once you are done, create
a pull request and tag the package maintainer as reviewer. **You won’t
be able to directly commit to `main` or merge without review/approval of
your PR.**

Changes to templates will **not** be reviewed by the main maintainers of
this repo, and will be merged directly, unless the PR author specifies
otherwise.
